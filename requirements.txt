# ── Core app ───────────────────────────────────────────────────────────────────
streamlit>=1.33,<2
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
aiohttp>=3.9

# ── AWS S3 ─────────────────────────────────────────────────────────────────────
boto3>=1.28
botocore>=1.31

# ── OpenAI + LlamaIndex (the code imports from llama_index.core, v0.10+) ──────
openai>=1.40.0

llama-index-core>=0.10.59,<0.11
llama-index-embeddings-openai>=0.1.11
llama-index-embeddings-huggingface>=0.1.6
llama-index-vector-stores-chroma>=0.1.10

# ChromaDB pinned to a stable series compatible with the above adapters
chromadb==0.4.24

# Hugging Face local embedding fallback
sentence-transformers>=2.6.1
# Torch is required by sentence-transformers; CPU wheels are fine on Streamlit Cloud
torch>=2.1.0

# ── Gemini (you still call google.generativeai for some paths) ─────────────────
google-generativeai>=0.7.2

# ── Optional: Playwright (headless browser extraction) ─────────────────────────
# If you plan to use the Playwright path in Streamlit Cloud, keep this.
# If you will only use the Requests fallback, you may comment these out.
playwright>=1.45
